# 实现进度

## 2026-04-28

### 通用化修正

本轮修正明确了系统与 `test-app` 的边界：

- `test-app/` 仅作为样例目标应用，不再作为生成逻辑的隐含模板。
- Playwright 生成器去除了 `/api/notes`、`Title`、`Content`、`Status` 等样例应用专用假设。
- 生成器改为基于 `api.scan` 和 `db.scan` 的结果推导测试：扫描到 `POST` 接口和数据模型时，会生成应用级数据写入验证。
- payload 按字段名自动生成，跳过常见服务端字段，如 `id`、`created_at`、`updated_at`。
- 写入后优先通过同集合 `GET` 接口回查，验证业务数据对目标应用真实生效。

### 数据库语义澄清

系统现在明确区分两类数据库：

- `.autotest/autotest.db`：自动化系统元数据库，只保存任务、事件、Agent Run、执行记录和报告索引。
- 目标应用数据库：由被测应用自己使用。测试里的“入表”指通过目标应用 API/UI 触发业务写入，让数据进入目标应用的隔离测试数据库。

用户需要负责将目标应用测试库与真实生产库隔离；自动化系统会把该测试库当作真实业务环境验证，而不是把业务数据写入系统元数据库。

### 测试与修复状态

- 失败修复分支中，补丁应用后如果重跑通过，任务最终状态现在为 `SUCCEEDED`。
- 如果诊断完成但重跑仍失败，才保留为 `PARTIAL_SUCCEEDED`。
- 新增测试覆盖通用数据写入生成逻辑，确保不会继续生成 `test-app` 专用的 `/api/notes` 路径。
- 新增测试覆盖修复重跑通过后的最终状态。

### 清理与重新运行

为便于完全重新跑系统，本轮已确认可安全清理的产物范围：

```powershell
Remove-Item -Recurse -Force .autotest, artifacts, .gocache -ErrorAction SilentlyContinue
Remove-Item -Force autotest.exe -ErrorAction SilentlyContinue
Remove-Item -Force test-app/*.db -ErrorAction SilentlyContinue
```

清理范围只包含系统元数据库、执行产物、Go 本地缓存、旧二进制和样例应用运行数据库，不包含源码、依赖描述和测试代码。

### 验证状态

Go 单元测试通过：

```powershell
$env:GOCACHE=(Resolve-Path .gocache).Path
$env:GOTELEMETRY='off'
go test ./...
```

## 2026-04-27

### 当前阶段

系统已经进入可运行 MVP 阶段，不再只是目录骨架或方案文档。

当前已接通：

- 项目扫描：`repo.scan`、`api.scan`、`db.scan`
- 场景规划：`scenario.plan`
- Playwright 测试生成
- Playwright 本地执行
- 失败诊断：`failure.diagnosis`
- 测试修复：`test.fix`
- Review Guard
- 测试补丁应用
- 重新执行
- SQLite 状态、事件、Agent Run 审计
- Agent 输入/输出 JSON artifact 落盘
- CLI 与 HTTP API

### 已清理的过时内容

- 删除历史运行 artifact。
- 删除手工 runner 检查输出。
- 删除 SQLite 备份文件。
- 删除带随机后缀的旧 Go 备份文件。
- 收敛 README 中过时的状态描述。

### 本次修正

真实 Playwright rerun 的失败点来自生成模板中的错误断言：

```text
scenario-summary should contain String(scenario.steps.length)
```

但页面 fixture 并没有渲染这个数字，因此该断言天然不稳定。现在生成器已改为只校验 `scenario.steps.length > 0`，避免继续生成无法通过的 DOM 断言。

随后又修复了多条 precondition 的断言方式：不再把列表项用空格拼成一个整体字符串，而是逐条断言每个 precondition 是否出现在 DOM 中。

### 继续清理

- 删除未被引用的旧本地占位修复器 `internal/tools/fixer`。
- 删除测试中兼容旧占位修复 marker 的断言分支。
- Playwright runner 的 JSON 请求解析已兼容 UTF-8 BOM。
- 验证产生的临时 artifact 已清理。

### 验证状态

Go 单元测试通过：

```powershell
$env:GOCACHE=(Resolve-Path .gocache).Path
$env:GOTELEMETRY='off'
go test ./...
```

真实 Playwright runner 已通过：

```text
mode: playwright
status: passed
```

CLI 工具注册表已验证：

```text
api.scan
db.scan
repo.scan
```

### 剩余工作

1. 让真实 Playwright 执行在当前仓库稳定跑绿。
2. 让失败诊断支持一次识别多个同类 spec 失败。
3. 让 `test.fix` 能返回多文件或模板级修复建议。
4. 将 Playwright trace、截图、stdout、stderr 汇总到报告 API。
5. 在当前 Orchestrator 之上补正式 Graph / Cycle 编排层。
